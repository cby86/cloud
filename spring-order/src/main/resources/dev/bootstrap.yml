spring:
  cloud:
    config:
      uri: http://localhost:8088/
      profile: dev
      name: auth
      username: user
      password: password123
      retry:
           initial-interval: 1000
           max-attempts: 6
           max-interval: 2000
           multiplier: 1.1
